name: Publish on Version Change

on:
  push:
    branches:
      - main
    paths:
      - 'pom.xml'
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Check if version changed
        id: version-check
        run: |
          # 获取当前版本
          CURRENT_VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
          echo "current-version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
          echo "📦 Current version: $CURRENT_VERSION"
          
          # 检查版本是否变化
          if git diff HEAD^ HEAD -- pom.xml | grep -q '<version>'; then
            echo "version-changed=true" >> $GITHUB_OUTPUT
            echo "✅ Version changed, will publish"
            
            # 检查是否为 SNAPSHOT 版本
            if [[ $CURRENT_VERSION == *-SNAPSHOT ]]; then
              echo "is-snapshot=true" >> $GITHUB_OUTPUT
              echo "🔄 SNAPSHOT version detected"
            else
              echo "is-snapshot=false" >> $GITHUB_OUTPUT
              echo "🎯 Release version detected"
            fi
          else
            echo "version-changed=false" >> $GITHUB_OUTPUT
            echo "⏭️  Version not changed, skipping"
          fi

      - name: Set up JDK 17
        if: steps.version-check.outputs.version-changed == 'true'
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Configure Maven Settings
        if: steps.version-check.outputs.version-changed == 'true'
        run: |
          mkdir -p ~/.m2
          cat > ~/.m2/settings.xml << EOF
          <settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
                    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                    xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
                                        http://maven.apache.org/xsd/settings-1.0.0.xsd">
            <servers>
              <server>
                <id>allbs</id>
                <username>${MAVEN_USERNAME}</username>
                <password>${MAVEN_PASSWORD}</password>
              </server>
              <server>
                <id>github</id>
                <username>${GITHUB_ACTOR}</username>
                <password>${GITHUB_TOKEN}</password>
              </server>
            </servers>
          </settings>
          EOF
        env:
          MAVEN_USERNAME: ${{ secrets.MAVEN_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Import GPG Key
        if: steps.version-check.outputs.version-changed == 'true'
        run: |
          echo "${{ secrets.GPG_PRIVATE_KEY }}" | base64 --decode | gpg --batch --import
          gpg --list-secret-keys --keyid-format LONG

      - name: Build and Test
        if: steps.version-check.outputs.version-changed == 'true'
        run: mvn clean verify -DskipTests

      - name: Publish to Maven Central
        if: steps.version-check.outputs.version-changed == 'true'
        env:
          MAVEN_USERNAME: ${{ secrets.MAVEN_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
        run: |
          echo "🚀 Publishing to Maven Central..."
          mvn deploy -Pgpg -DskipTests \
            -Dgpg.passphrase="${GPG_PASSPHRASE}"

      - name: Publish to GitHub Packages
        if: steps.version-check.outputs.version-changed == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "🚀 Publishing to GitHub Packages..."
          mvn deploy -DskipTests \
            -DaltDeploymentRepository=github::default::https://maven.pkg.github.com/${{ github.repository }}

      - name: Create GitHub Release
        if: steps.version-check.outputs.version-changed == 'true' && steps.version-check.outputs.is-snapshot == 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version-check.outputs.current-version }}
          name: Release v${{ steps.version-check.outputs.current-version }}
          generate_release_notes: true
          make_latest: true
          body: |
            ## 📦 Maven 依赖

            ```xml
            <dependency>
                <groupId>cn.allbs</groupId>
                <artifactId>allbs-excel</artifactId>
                <version>${{ steps.version-check.outputs.current-version }}</version>
            </dependency>
            ```

            ## 📥 下载

            - [Maven Central](https://central.sonatype.com/artifact/cn.allbs/allbs-excel/${{ steps.version-check.outputs.current-version }})
            - [GitHub Packages](https://github.com/${{ github.repository }}/packages)
          files: |
            target/allbs-excel-*.jar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Summary
        if: steps.version-check.outputs.version-changed == 'true'
        run: |
          echo "## ✅ 发布成功" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**版本**: ${{ steps.version-check.outputs.current-version }}" >> $GITHUB_STEP_SUMMARY
          echo "**类型**: ${{ steps.version-check.outputs.is-snapshot == 'true' && 'SNAPSHOT' || 'Release' }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 仓库链接" >> $GITHUB_STEP_SUMMARY
          echo "- [Maven Central](https://central.sonatype.com/artifact/cn.allbs/allbs-excel/${{ steps.version-check.outputs.current-version }})" >> $GITHUB_STEP_SUMMARY
          echo "- [GitHub Packages](https://github.com/${{ github.repository }}/packages)" >> $GITHUB_STEP_SUMMARY

